diff --git a/src/models/gptneox.cpp b/src/models/gptneox.cpp
index 33ebe2d..80291b9 100644
--- a/src/models/gptneox.cpp
+++ b/src/models/gptneox.cpp
@@ -1,5 +1,40 @@
 #include "models.h"
 
+#include <algorithm>
+#include <cstdlib>
+
+namespace {
+
+static bool cai_env_enabled(const char * name) {
+    const char * value = std::getenv(name);
+    if (value == nullptr) {
+        return false;
+    }
+    return value[0] != '\0' && value[0] != '0';
+}
+
+static int cai_env_i32(const char * name, int fallback) {
+    const char * value = std::getenv(name);
+    if (value == nullptr || value[0] == '\0') {
+        return fallback;
+    }
+
+    char * end = nullptr;
+    const long parsed = std::strtol(value, &end, 10);
+    if (end == value) {
+        return fallback;
+    }
+    if (parsed < 0) {
+        return 0;
+    }
+    if (parsed > 2147483647L) {
+        return 2147483647;
+    }
+    return static_cast<int>(parsed);
+}
+
+} // namespace
+
 void llama_model_gptneox::load_arch_hparams(llama_model_loader & ml) {
     ml.get_key(LLM_KV_ATTENTION_LAYERNORM_EPS, hparams.f_norm_eps);
     ml.get_key(LLM_KV_USE_PARALLEL_RESIDUAL,   hparams.use_par_res);
@@ -91,6 +126,18 @@ llama_model_gptneox::graph::graph(const llama_model & model, const llm_graph_params & params) : llm_graph_context(params) {
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
+    const bool cai_layer_range_enabled = cai_env_enabled("CAI_LLAMA_CPP_SHARD_LAYER_RANGE");
+    const int  cai_layer_start = cai_layer_range_enabled
+        ? std::min<int>(static_cast<int>(n_layer), cai_env_i32("CAI_LLAMA_CPP_SHARD_LAYER_START", 0))
+        : 0;
+    const int  cai_layer_end = cai_layer_range_enabled
+        ? std::min<int>(static_cast<int>(n_layer), cai_env_i32("CAI_LLAMA_CPP_SHARD_LAYER_END", static_cast<int>(n_layer)))
+        : static_cast<int>(n_layer);
+
+    if (cai_layer_range_enabled) {
+        GGML_ASSERT(cai_layer_end > cai_layer_start);
+    }
+
     inpL = build_inp_embd(model.tok_embd);
 
     // inp_pos - contains the positions
@@ -100,7 +147,7 @@ llama_model_gptneox::graph::graph(const llama_model & model, const llm_graph_params & params) : llm_graph_context(params) {
 
     ggml_tensor * inp_out_ids = build_inp_out_ids();
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = cai_layer_start; il < cai_layer_end; ++il) {
         cur = build_norm(inpL,
                 model.layers[il].attn_norm,
                 model.layers[il].attn_norm_b,
@@ -133,7 +180,8 @@ llama_model_gptneox::graph::graph(const llama_model & model, const llm_graph_params & params) : llm_graph_context(params) {
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        const int cai_output_layer = cai_layer_range_enabled ? cai_layer_end - 1 : static_cast<int>(n_layer) - 1;
+        if (il == cai_output_layer && inp_out_ids) {
             cur  = ggml_get_rows(ctx0,  cur, inp_out_ids);
             inpL = ggml_get_rows(ctx0, inpL, inp_out_ids);
         }
@@ -201,6 +249,16 @@ llama_model_gptneox::graph::graph(const llama_model & model, const llm_graph_params & params) : llm_graph_context(params) {
         }
     }
 
+    if (cai_layer_range_enabled && cai_layer_end < n_layer) {
+        // Expose the raw layer boundary activation instead of running the
+        // output norm/head. CAI policy decides admission through the proof
+        // registry after a model-backed equivalence probe.
+        cb(inpL, "cai_layer_range_activation", cai_layer_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = build_norm(inpL,
             model.output_norm,
             model.output_norm_b,
